# Postgres (async driver)
DATABASE_URL=postgresql+psycopg_async://trails:trailspwd@127.0.0.1:54322/trails

# Where to fetch JWKS from auth-svc (change host if in docker compose)
AUTH_JWKS_URL=http://localhost:8001/auth/jwks

# token issuer (optional sanity)
TOKEN_ISSUER=authentication-svc

# Invitations
INVITE_SECRET=change-this-to-a-long-random-string
INVITE_TTL_HOURS=168        # 7 days default
INVITE_BASE_URL=http://localhost:8002/invites